# The add-on runs the prebuilt multi-arch image; the arch resolves from the manifest.
ARG BUILD_FROM
FROM ghcr.io/evanscastonguay/bhyve-xd-ble:latest
# (BUILD_FROM is provided by the Supervisor but intentionally unused — we ship a ready image.)
